query getCharacters($page: Int = 1, $filter: FilterCharacter){
    characters(page: $page, filter: $filter) {
        results {
            image
            id
            name
            location {
                name
            }
        }
    }
}
